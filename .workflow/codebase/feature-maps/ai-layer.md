# FT-010 — AI Layer

| Field | Value |
|-------|-------|
| **ID** | FT-010 |
| **Status** | partial |
| **Phase** | Phase 3 partial |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-003 | StrategyLayer | L3-strategy | `quantflow/strategy/ai_factors.py`, `quantflow/strategy/templates/ml_ensemble.py`, `quantflow/strategy/sentiment.py` |

## Description

- **ai_factors.py** — `quantflow/strategy/ai_factors.py:57`. `AIFactorEngine`: Meta-Labeling (second-stage model filter), tree-model feature importance, expanding CV splits. Exported via `strategy/__init__.py`; consumed by `MLEnsembleStrategy`. Multiple unit tests in `tests/unit/test_ai_factors.py`.
- **MLEnsembleStrategy** — `templates/ml_ensemble.py:72`. Active ML ensemble using 21 factors as features + meta-labeling; `train_model()` then `generate_signals()` uses `predict_proba` > threshold. Registered in `strategy/catalog.py`.
- **FinBERT sentiment** — `quantflow/strategy/sentiment.py`. **Implemented**: `SentimentAnalyzer` (ProsusAI/finbert, positive/negative/neutral scoring, lazy-loaded transformers/torch with graceful degradation to neutral 0.33 when deps absent), `NewsCollector` (CryptoPanic/CoinDesk RSS via feedparser, async `fetch_news`). Unit tests in `tests/unit/test_sentiment.py` (296 lines). **Not exported** from `strategy/__init__.py`; **no CLI wiring** — callable only via direct import.
- **Qlib RD-Agent** — **Not implemented**. `ai_factors.py:5` header comment claims "Qlib-style RD-Agent integration is via the CLI research workflow" but this is **inaccurate** — no qlib code, no CLI `research` path invokes it. `qlib>=0.9` is an optional `[ml]` extra only. Planned in blueprint `epics.md` E13-S1 (acceptance: RD-Agent generates 5+ factors with IC > 0.03).

## Notes

- Duplicate `_positive_class_probability` / split helpers in `ml_ensemble.py:18-69` + `ai_factors.py:20-28` — dedup candidate.
- ⚠️ `ai_factors.py:5` comment misrepresents CLI integration — should be corrected (Qlib RD-Agent is planned, not wired).
- ⚠️ `SentimentAnalyzer`/`NewsCollector` implemented + tested but not exported from package `__all__` and not wired to any CLI command — discovery gap.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
