# FT-010 — AI Layer

| Field | Value |
|-------|-------|
| **ID** | FT-010 |
| **Status** | partial |
| **Phase** | Phase 3 partial (MIXED) |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-003 (StrategyLayer) | ai_factors, ml_ensemble, sentiment, rd_agent |

## Description

ML/meta-labeling, sentiment, and automated factor mining — uneven maturity across three sub-components.

- **`AIFactorEngine` (Meta-Labeling) — EXPORTED but NOT integrated.** `strategy/ai_factors.py` provides `meta_label`/`compute_factor`/`feature_selection` (sklearn). Listed in `strategy/__init__.py __all__`, but no strategy or CLI calls it; `MLEnsembleStrategy` uses its own internal meta-labeling.
- **`MLEnsembleStrategy` — ACTIVE.** `strategy/templates/ml_ensemble.py`: 21 factors + GradientBoosting primary + triple-barrier meta-labeling filter, expanding-window OOS, fail-closed reject-all. Registered (`ml_ensemble`); requires trained joblib model.
- **FinBERT sentiment — IMPLEMENTED but NOT exported / NOT CLI-wired.** `strategy/sentiment.py` (`SentimentAnalyzer`, `NewsCollector`) referenced only by itself; absent from all `__init__.__all__` and CLI. Orphaned code.
- **Qlib RD-Agent CLI skeleton — ACTIVE with dependency guard.** `strategy/rd_agent.py` exposes `quantflow ai rdagent`; `RDAgentRunner.check_available()` probes `qlib`, fails fast with install hint (`pip install -e ".[ml]"`) when absent; when present runs Alpha158-style IC evaluation. Full LLM factor search = future (blueprint E13-S1).

Status: `MLEnsembleStrategy` + RD-Agent CLI = active; `AIFactorEngine` = exported but unused; FinBERT = implemented but unexported/unwired.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
