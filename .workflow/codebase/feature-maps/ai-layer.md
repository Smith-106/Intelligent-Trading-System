# FT-010 — AI Layer

| Field | Value |
|-------|-------|
| **ID** | FT-010 |
| **Status** | partial |
| **Phase** | Phase 3 partial |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-003 | StrategyLayer | L3-strategy | `quantflow/strategy/ai_factors.py`, `quantflow/strategy/templates/ml_ensemble.py`, `quantflow/strategy/sentiment.py` |

## Description

- **ai_factors.py** — `quantflow/strategy/ai_factors.py:20`. `AIFactorEngine`: Meta-Labeling (second-stage model filter), tree-model feature importance, expanding CV splits. File header notes: "Qlib-style RD-Agent integration via CLI research workflow."
- **MLEnsembleStrategy** — `templates/ml_ensemble.py:72`. Active ML ensemble using 21 factors as features + meta-labeling; `train_model()` then `generate_signals()` uses `predict_proba` > threshold.
- **FinBERT / Qlib RD-Agent** — **No implementation files found in `quantflow/`** — planned/optional (referenced only in CLAUDE.md). Follow up if Phase 3 scope.

## Notes

- Duplicate `_positive_class_probability` / split helpers in `ml_ensemble.py:18-69` + `ai_factors.py:20-28` — dedup candidate.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
