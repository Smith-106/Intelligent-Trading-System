# FT-004 — Anti-Overfitting Validation

| Field | Value |
|-------|-------|
| **ID** | FT-004 |
| **Status** | active |
| **Phase** | Phase 2 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-003 | StrategyLayer | L3-strategy | `quantflow/strategy/validation/` |

## Description

| Method | Function:line | Pass criterion |
|--------|---------------|----------------|
| **CPCV** | `cpcv.py:117` (`cpcv_backtest`), `split_cpcv` `:58` | PBO < 0.5; supports train-window optimization |
| **DSR** | `dsr.py:18` (`deflated_sharpe_ratio`), `_expected_max_sharpe` `:78` | DSR > 0.95 |
| **PBO** | `pbo.py:39` (`probability_of_overfitting`) | Overfit path ratio; rank correlation |
| **WFO** | `wfo.py:251` (`walk_forward_optimization`) | OOS efficiency > 50%; rolling + anchored; OOS param regeneration |
| **GO/NO-GO Gate** | `gate.py:26` (`validation_gate`) | Combined CPCV + DSR + WFO decision gate |
| **Triple Barrier** | `barriers.py:18` (`triple_barrier_labels`) | Event labeling |
| **Min TRL** | `barriers.py:68` (`minimum_track_record_length`) | Min track record length |
| **Signal Quality** | `signal_quality.py:11` (`signal_quality_metrics`), `:71` (`aggregate_signal_quality`) | Precision/recall/hit-rate/Brier/OOS Sharpe |

Each stage accepts `signal_fn` + `param_space` for true OOS re-optimization (not in-sample params reused). Validation pipeline per CLAUDE.md: `VectorBT快速筛选 → Optuna精调 → CPCV多路径 → DSR修正 → WFO前向 → GO/NO-GO`.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
