# FT-004 — Anti-Overfitting Validation

| Field | Value |
|-------|-------|
| **ID** | FT-004 |
| **Status** | active |
| **Phase** | Phase 2 complete |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-003 (StrategyLayer) | `strategy/validation/` suite |

## Description

Layered validation suite to gate deployment on genuine edge rather than backtest overfitting. All under `quantflow/strategy/validation/`.

- **CPCV** (`cpcv.py`): Combinatorial Purged CV with embargo; `passed` when `pbo < 0.5`.
- **DSR** (`dsr.py`): Deflated Sharpe Ratio; `passed` when `dsr > 0.95` (fail-closed if no finite OOS Sharpe).
- **PBO** (`pbo.py`): stand-alone Probability of Backtest Overfitting.
- **WFO** (`wfo.py`): Walk-Forward Optimization (rolling + anchored); `passed` when OOS efficiency `> 0.5`.
- **GO/NO-GO gate** (`gate.py`): `validation_gate()` runs CPCV → DSR → WFO; all must pass for `GO`.
- **Triple-barrier labeling** (`barriers.py`): `triple_barrier_labels()` + `minimum_track_record_length()`.
- **Look-ahead detection** (`lookahead.py`): static AST scan for masked-aggregation future-leak patterns; CLI `--method lookahead`.
- **Monte Carlo stress** (`monte_carlo.py`): `trade_shuffle_stress` + `returns_bootstrap_stress` — diagnostic, non-gating.
- **Signal quality** (`signal_quality.py`): precision/recall/hit-rate/brier/OOS Sharpe.

All exposure thresholds match spec (PBO<0.5, DSR>0.95, WFO OOS>50%).

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
