# FT-006 — Risk Controls

| Field | Value |
|-------|-------|
| **ID** | FT-006 |
| **Status** | active |
| **Phase** | Phase 2 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-004 | SignalRiskLayer | L4-signal-risk | `quantflow/signal/` |

## Description

- **RiskEngine** — `risk_engine.py:15`. Multi-layer short-circuit pipeline (`:43`): position_limit → portfolio_limit → strategy_budget → daily_loss → weekly_loss → drawdown → VaR. First failure short-circuits (`:52`). Logs risk events to Prometheus. Per-strategy budget via `strategy_id_constituents`.
- **PositionSizer** — `position_sizer.py:12`. Half-Kelly (`kelly_fraction=0.5`) × `signal.strength`, clamped by `max_position_pct`.
- **risk_metrics** — `risk_metrics.py`: `value_at_risk` (`:13`), `conditional_var` (CVaR `:49`), `max_drawdown` (`:64`), `sharpe_ratio` (`:81`), `sortino_ratio` (`:97`), `calmar_ratio` (`:114`).
- **PortfolioManager** — `portfolio.py:13`. Weekly PnL passed to RiskEngine.
- **SignalGenerator** — `generator.py:12`. **WaveSignalGenerator** — `wave_signal_generator.py:58` (invalidation checker `:132`, `InvalidationEvent`/`InvalidationSeverity`).

`ScalingPosition → RiskEngine` protocol: `ScalingPositionSizer` (L5) outputs `PositionRequest`, `RiskEngine` is the final authority gate. `RiskConfig.kelly_fraction` + `var_confidence` now loaded from YAML (previously hardcoded — silent-drop anti-pattern, fixed).

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
