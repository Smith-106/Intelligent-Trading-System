# FT-005 — Execution

| Field | Value |
|-------|-------|
| **ID** | FT-005 |
| **Status** | active |
| **Phase** | Phase 3 complete |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-005 (ExecutionLayer) | Gateways, engine, order router/manager, position manager, KillSwitch |

## Description

Order routing and fill management across three run modes with a fail-closed emergency stop.

- **Run modes**: `paper` (PaperGateway, no API key), `sandbox` (OKX testnet), `live` (OKX real).
- **`GatewayBase`** (`gateway_base.py`): ABC with `connect/send_order/cancel_order/query_positions` + `GatewayError`.
- **`ExecutionEngine`** (`engine.py`): kill-switch gate → route → track → metric → event → fill; integrates with EventBus (ORDER/FILL).
- **`OrderRouter`** (`order_router.py`): gateway dispatch + `build_order`/`build_close_request` (reduceOnly) — extracted from engine (SRP).
- **`OrderManager`** (`order_manager.py`): lifecycle + timeout tracking.
- **`KillSwitch`** (`kill_switch.py`): `activate()` cancels all orders, flattens with reduceOnly market orders, blocks new submissions; fail-closed.
- **`PositionManager`** (`position_manager.py`): per-route reconciliation.
- Gateways: `OKXGateway` (CCXT async, spot/swap, reconnect), `PaperGateway` (local sim).
- OKX creds from env only; KillSwitch mandatory in live mode.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
