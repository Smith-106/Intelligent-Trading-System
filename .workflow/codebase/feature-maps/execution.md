# FT-005 — Execution

| Field | Value |
|-------|-------|
| **ID** | FT-005 |
| **Status** | active |
| **Phase** | Phase 3 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-005 | ExecutionLayer | L5-execution | `quantflow/execution/` |

## Description

- **ExecutionEngine** — `engine.py:30`. Routes orders across paper/live(OKX)/sandbox gateways; emits ORDER and FILL events; latency/fill Prometheus metrics.
- **GatewayBase** (ABC) — `gateway_base.py:15`. `connect`/`send_order`/`cancel_order`/`query_positions`.
- **PaperGateway** — `paper_gateway.py:14`. Local simulation, no API key.
- **OKXGateway** — `okx_gateway.py:18`. CCXT async + reconnect, sandbox/live.
- **OrderManager** — `order_manager.py:15`. Order state tracking + timeout.
- **PositionManager** — `position_manager.py:12`.
- **KillSwitch** — `kill_switch.py:14`. Emergency stop: cancel pending, market-flatten, block new submissions.
- **ScalingPositionSizer** — `scaling_position_sizer.py:68`. Phased position building; outputs `PositionRequest` (`:31`) for RiskEngine authority. `ScalingConfig` `:52`, `PositionPhase` enum `:21`.

Execution modes: `paper` / `sandbox` (OKX testnet) / `live` (OKX). CLI `run` loads OKX creds from env (`main.py:61`). **⚠️ Kill switch not enforced in live mode** — config flag only, no assertion (gap vs CLAUDE.md requirement).

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
