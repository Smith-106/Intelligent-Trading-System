# FT-005 — Execution

| Field | Value |
|-------|-------|
| **ID** | FT-005 |
| **Status** | active |
| **Phase** | Phase 3 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-005 | ExecutionLayer | L5-execution | `quantflow/execution/` |

## Description

- **ExecutionEngine** — `engine.py:30`. *Orchestrates* the submit pipeline (kill-switch gate → route → track → metric → event → fill handling) and owns gateway lifecycle; routing/order-construction delegated to `OrderRouter` (ISS-003). Emits ORDER and FILL events; latency/fill Prometheus metrics.
- **OrderRouter** — `order_router.py:34` (ISS-003). Gateway dispatch + Order/Request construction: `route()`→gateway.send_order, `build_order(request)`→Order, `build_close_request(position)`→opposing reduceOnly OrderRequest, `is_closeable(position)` POSITION_EPSILON guard. arch-017 lazy `set_gateway` binding (constructed unbound, rebound in `ExecutionEngine.start`).
- **GatewayBase** (ABC) — `gateway_base.py:15`. `connect`/`send_order`/`cancel_order`/`query_positions`.
- **PaperGateway** — `paper_gateway.py:14`. Local simulation, no API key.
- **OKXGateway** — `okx_gateway.py:50`. CCXT async + reconnect, sandbox/live. ISS-005 `market_type` spot/swap ctor param (default `spot`, validates spot|swap); connect honors `options.defaultType`; `query_positions` dual-branch — `_query_swap_positions` reads contracts/entryPrice/markPrice/unrealizedPnl, `_query_spot_positions` derives non-quote assets from `fetch_balance` (entry_price=0/unrealized_pnl=0). ISS-011 `monitoring_sink` ctor param emits gateway connected/disconnect/reconnect via `_record_disconnect` helper.
- **OrderManager** — `order_manager.py:30`. Order state tracking + timeout. ISS-011 `monitoring_sink` ctor param; `check_timeouts` emits `record_order_timed_out`.
- **PositionManager** — `position_manager.py:12`.
- **KillSwitch** — `kill_switch.py:14`. Emergency stop: cancel pending, market-flatten, block new submissions.

Execution modes: `paper` / `sandbox` (OKX testnet) / `live` (OKX). CLI `run` loads OKX creds from env (`main.py:61`). `ExecutionConfig.market_type` (default `spot`, `config/default.yaml` execution.market_type) drives OKXGateway spot/swap branch. **⚠️ Kill switch not enforced in live mode** — config flag only, no assertion (gap vs CLAUDE.md requirement).

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
