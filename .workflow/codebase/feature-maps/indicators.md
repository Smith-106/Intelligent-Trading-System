# FT-009 — Indicators (27 factors)

| Field | Value |
|-------|-------|
| **ID** | FT-009 |
| **Status** | active |
| **Phase** | Phase 1 complete |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-002 (IndicatorsLayer) | IndicatorEngine, FactorRegistry, MarketRegimeDetector |

## Description

Pure pandas/numpy indicator library (no external TA dependency) + market-regime detector for strategy gating.

- **`IndicatorEngine`** (`indicators/engine.py`): `batch_calculate`/`compute_all`; `FACTOR_NAMES` enumerates **27 factors**:
  - Trend (7): `sma_20, sma_50, ema_12, ema_26, macd, macd_signal, macd_histogram`
  - Momentum (4): `rsi_14, stoch_k, stoch_d, williams_r_14`
  - Volatility (5): `atr_14, bb_upper, bb_middle, bb_lower, adx_14`
  - Volume (5): `obv, vwap, mfi_14, volume_sma_20, volume_ratio`
  - Elliott Wave (6): `zigzag_pivots, wave_count, fibonacci_levels, critical_levels, wave_channel, divergence`
- **`MarketRegimeDetector`** (`indicators/regime.py`): ADX-based trending vs mean-reversion (+ BB-width / ATR-percentile); gates strategies on `on_bar` path only (vectorized `generate_signals` intentionally not regime-gated — documented divergence in `base.py`).

Status: complete (21 base + 6 Elliott = 27).

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
