# FT-001 — Strategies

| Field | Value |
|-------|-------|
| **ID** | FT-001 |
| **Status** | active |
| **Phase** | Phase 1/2 complete (7 strategies) |

## Requirements

(None mapped — no `.workflow/blueprint/` requirements.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-003 | StrategyLayer | L3-strategy | `quantflow/strategy/base.py`, `quantflow/strategy/templates/`, `quantflow/strategy/catalog.py` |

## Description

7 strategies via `StrategyBase` dual-mode API (vectorized `generate_signals(df)` + incremental `on_bar(ctx, bar)`), YAML-driven via `strategy/catalog.py` factory:

| Strategy | File | Logic |
|----------|------|-------|
| trend_following | `templates/trend_following.py:31` | Fast/slow MA cross + MACD histogram + RSI + ATR cap + volume filter; `min_conditions` majority vote; trailing stop + pct take-profit exit |
| mean_reversion | `templates/mean_reversion.py:24` | RSI overbought/oversold + Bollinger breakout + volume confirm; incremental `on_bar` for live/paper |
| elliott_wave | `templates/elliott_wave.py:19` | Pure rule engine on `elliott_wave()` labels; enter W2/W4/WC, exit W5; W5-vs-W3 divergence via `wave_momentum_divergence`; zigzag + fibonacci |
| volatility_breakout (P1) | `templates/volatility_breakout.py:31` | ATR + Bollinger + Keltner; low→high vol regime shift; ATR squeeze / BB-mid exit |
| funding_rate (P2) | `templates/funding_rate.py:17` | 8h perp funding-rate extremes + OI confirm; exit on rate→neutral / OI reversal |
| momentum_rotation (P3) | `templates/momentum_rotation.py:17` | Cross-sectional momentum rank top-N; `generate_cross_sectional_signals()`; rebalance + stop |
| ml_ensemble (P4) | `templates/ml_ensemble.py:72` | Nonlinear ensemble on 21 factors; Meta-Labeling 2nd-stage filter; `predict_proba` > threshold |

YAML configs: `quantflow/config/strategies/*.yaml` (7 files). Shared runtime: `templates/_runtime.py` (rolling helpers).

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
