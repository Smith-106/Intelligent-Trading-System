# FT-001 — Strategies

| Field | Value |
|-------|-------|
| **ID** | FT-001 |
| **Status** | active |
| **Phase** | Phase 1/2 complete (7 strategies) |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-003 (StrategyLayer) | 7 strategy templates + `StrategyBase` dual-mode API + catalog |

## Description

Seven trading strategies share one dual-mode API (`StrategyBase`): a vectorized `generate_signals(df) -> (entries, exits)` path for research/backtest, and a stateful `on_bar(ctx, bar)` / `on_init` / `on_tick` path for live/paper trading (signal emitted via `ctx.emit_signal`). YAML-driven, discovered through `strategy/catalog.py`.

| Strategy | Phase | File |
|---|---|---|
| `trend_following` | P1 | `quantflow/strategy/templates/trend_following.py` |
| `mean_reversion` | P1 | `quantflow/strategy/templates/mean_reversion.py` |
| `elliott_wave` | P1 | `quantflow/strategy/templates/elliott_wave.py` |
| `volatility_breakout` | P1 | `quantflow/strategy/templates/volatility_breakout.py` |
| `funding_rate` | P2 | `quantflow/strategy/templates/funding_rate.py` |
| `momentum_rotation` | P3 | `quantflow/strategy/templates/momentum_rotation.py` |
| `ml_ensemble` | P4 | `quantflow/strategy/templates/ml_ensemble.py` |

Note: `ml_ensemble` requires a pre-trained joblib model; uses its own internal triple-barrier meta-labeling (does NOT use `AIFactorEngine`). All 7 selectable via `run/research/optimize/validate --strategy` and listed by `quantflow status`.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
