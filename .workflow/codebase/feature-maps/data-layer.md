# FT-007 — Data Layer

| Field | Value |
|-------|-------|
| **ID** | FT-007 |
| **Status** | active |
| **Phase** | Phase 1 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-001 | DataLayer | L1-data | `quantflow/data/` |

## Description

- **DataFetcher** — `fetcher.py:27`. CCXT async fetcher.
- **DataStore** — `store.py:39`. Parquet Hive-partitioned (symbol/year/month) + DuckDB query; `validate_symbol`/`validate_columns` guards at `:29`/`:34` (delegated to `quantflow/common/validators.py`).
- **FeatureStore** — `feature_store.py:17`. Point-in-time safe (`compute_features` `:25` queries only up to `end=timestamp`); Parquet + DuckDB.
- **RedisCache** — `redis_cache.py:16`. WebSocket bar in-memory cache.
- **MTFAligner** — `mtf_aligner.py:43` (`MTFData` `:33`). Multi-timeframe alignment.
- **cleaner** — `cleaner.py:13` (`clean_ohlcv`), `validate_no_future_leak` `:108` (anti-look-ahead check).

`validate_symbol` / `validate_columns` from `common/validators.py` gate every symbol→path/glob site (read, write, in-place transform) per anti-path-traversal spec (REV-008). `feature_store.py:32` enforces point-in-time queries to prevent look-ahead data leakage.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
