# FT-007 — Data Layer

| Field | Value |
|-------|-------|
| **ID** | FT-007 |
| **Status** | active |
| **Phase** | Phase 1 complete (P0 leak-safe hardening code-complete-await-verify) |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-001 (DataLayer) | Fetcher, store, FeatureStore, RedisCache, MTFAligner, cleaner |

## Description

Ingestion, storage, feature management, and leak-safe utilities.

- **CCXT async OKX fetcher** (`data/fetcher.py`): `connect`/`fetch_ohlcv` (pagination, non-finite rejection, `CALL_TIMEOUT`), WebSocket/`stream_bars`; sandbox mode.
- **Parquet Hive + DuckDB** (`data/store.py`): Hive-partitioned Parquet (`symbol/year/month`, zstd), DuckDB zero-copy query with path-traversal + SQL-injection guards.
- **FeatureStore** (`data/feature_store.py`): point-in-time safe (`end=timestamp`, no future leak).
- **RedisCache** (`data/redis_cache.py`): ticker/bar cache with TTL; raises `DataError` when unconnected.
- **MTFAligner** (`data/mtf_aligner.py`): multi-timeframe (1W→4H→1H→15m) with leak-safe HTF shift.
- **`clean_ohlcv` / `validate_no_future_leak`** (`data/cleaner.py`): dedup, gap-fill, OHLC repair, outlier z-score, future-timestamp rejection.

Leak-safety engineered at every layer (fetch parse, store query, feature compute, MTF align, cleaner).

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
