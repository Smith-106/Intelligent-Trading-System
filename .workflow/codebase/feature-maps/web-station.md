# FT-008 — QuantFlow Station Web UI

| Field | Value |
|-------|-------|
| **ID** | FT-008 |
| **Status** | active |
| **Phase** | New (beyond CLAUDE.md) |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-009 | WebStation | web-presentation | `quantflow/web/` |

## Description

aiohttp web frontend with 23 REST endpoints at `app.py:238` (`create_app`), launched by CLI `station` command at `:1034` (`run_station` `:290`).

**Routes** (`app.py:264-285`):
- `GET /` — static index.html
- `GET /api/overview`, `GET /api/strategies`, `GET /api/data` (snapshot)
- `POST /api/data/download`, `POST /api/data/seed-demo`, `POST /api/data/tag-source`
- `POST /api/research`, `GET /api/research/history`
- `POST /api/validate`, `GET /api/validate/history`
- `GET|POST /api/workbench/state` (persisted workbench state)
- `GET /api/monitoring`, `GET /api/execution`
- `GET /api/session`, `GET /api/session/events`, `GET /api/session/history`
- `POST /api/session/start`, `POST /api/session/stop`, `POST /api/session/kill-switch`
- `GET /static/*` (static assets)

**Security model** (`web/security.py`):
- `same_origin_guard` — CSRF same-origin middleware (`app.py:254`)
- `_is_loopback_host(host)` + `_station_token()` — bind-boundary guard: non-loopback bind requires `QUANTFLOW_STATION_TOKEN` env, else `run_station` refuses start (`:299-304`)
- `create_app` is host-agnostic (docstring `:244-253`); tests construct app directly
- Supports: `StationService` (`web/service.py`), `StationSessionManager` (`web/session_manager.py`), `StationHistoryStore` (`web/history.py`)

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
