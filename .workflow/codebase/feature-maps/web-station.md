# FT-008 — QuantFlow Station Web UI

| Field | Value |
|-------|-------|
| **ID** | FT-008 |
| **Status** | active |
| **Phase** | Active (beyond baseline) |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-009 (WebStation) | aiohttp app, security, service, session manager, history, rate limit |

## Description

Local-first aiohttp control surface for the whole system. Verified 21 API routes (+ index + static) across 9 groups:

- overview — `GET /api/overview`
- strategies — `GET /api/strategies`
- data — `GET /api/data`, `POST /api/data/download`, `POST /api/data/seed-demo`, `POST /api/data/tag-source`
- research — `POST /api/research`, `GET /api/research/history`
- validate — `POST /api/validate`, `GET /api/validate/history`
- workbench — `GET|POST /api/workbench/state`
- monitoring — `GET /api/monitoring`
- execution — `GET /api/execution`
- session — `GET /api/session`, `/events`, `/history`, `POST /start`, `/stop`, `/kill-switch`

**Security**: `same_origin_guard` middleware applies Bearer shared-secret auth (`QUANTFLOW_STATION_TOKEN`, `hmac.compare_digest`) + CSRF Origin↔Host match to all mutation methods; `X-Requested-With` explicitly NOT trusted. Bind-boundary launch guard: `run_station` refuses non-loopback bind without token. Extras: 256 KiB body cap, per-IP rate-limit, path/secret redaction at HTTP boundary.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
