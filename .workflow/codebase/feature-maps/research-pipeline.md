# FT-003 — Research Pipeline

| Field | Value |
|-------|-------|
| **ID** | FT-003 |
| **Status** | active |
| **Phase** | Phase 1 complete |

## Requirements

(None mapped.)

## Components

| ID | Name | Type | Locations |
|----|------|------|-----------|
| TC-003 | StrategyLayer | L3-strategy | `quantflow/strategy/research/` |

## Description

- **BacktestEngine** — `research/backtest.py:58` (`BacktestResult` dataclass at `:21`). VectorBT-style vectorized backtest. **Renamed from `VectorBTEngine`** — pure pandas/numpy, no VectorBT dependency (Py3.14+ numba incompat). New code referencing `VectorBTEngine` raises ImportError.
- **StrategyOptimizer** — `research/optimizer.py:19`. Optuna-driven optimization, `bayesian` / `cmaes` / `grid` methods.
- **generate_report** — `research/report.py:8`. Text/Markdown report formatting.
- **elliott_wave_backtest** — `research/elliott_wave_backtest.py:23`. Dedicated EW backtest with `generate_synthetic_wave_data` (`:49`), `run_backtest` (`:147`), MACD histogram (`:271`), RSI (`:278`).

Pipeline (per CLAUDE.md): `策略参数 → 快速筛选 → Optuna精调 → CPCV → DSR → WFO → GO/NO-GO`.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
