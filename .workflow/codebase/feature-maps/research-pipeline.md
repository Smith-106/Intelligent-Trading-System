# FT-003 — Research Pipeline

| Field | Value |
|-------|-------|
| **ID** | FT-003 |
| **Status** | active |
| **Phase** | Phase 1 complete |

## Requirements

None tracked in `.workflow/blueprint` (no SPEC/REQ files present).

## Components

| Component | Role |
|-----------|------|
| TC-003 (StrategyLayer) | `BacktestEngine`, `StrategyOptimizer`, report generation |

## Description

Pure vectorized backtesting and parameter optimization replacing the original VectorBT dependency (incompatible with Python 3.14+/numba).

- **`BacktestEngine`** (`strategy/research/backtest.py`): vectorized long/short backtest on whole-bar entries/exits with fee model; outputs `BacktestResult` (Sharpe/Sortino/Calmar/MaxDD/win-rate/profit-factor/trades + equity/drawdown curves). Bar-frequency inferred for intraday annualization. Reused by CPCV/WFO/MC stress.
- **`StrategyOptimizer`** (`strategy/research/optimizer.py`): Optuna samplers — `bayesian` (GPSampler w/ TPESampler fallback), `cmaes` (CmaEsSampler), deterministic local `grid`; objectives sharpe/sortino/calmar/return/win_rate.
- **Report** (`strategy/research/report.py`): `generate_report` → text/markdown summary.

Status: fully implemented; no VectorBT dependency remains.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
