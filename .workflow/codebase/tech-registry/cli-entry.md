# TC-008 — CliEntry

| Field | Value |
|-------|-------|
| **ID** | TC-008 |
| **Type** | cli |
| **Features** | FT-002 (CLI) |

## Code Locations

- `quantflow/cli/main.py` — Typer + Rich app (`no_args_is_help=True`, rich markup mode L47-52); 8 commands
- `quantflow/cli/__init__.py`

## Exported Symbols

`app`, `download`, `research`, `optimize`, `validate`, `run`, `benchmark`, `station`, `status`, `_load_gateway_config_from_env`

## Dependencies

- **Imports**: `common`, `data`, `strategy`, `execution`, `indicators`, `monitoring`, `web`. Composes all layers (entry point).
- **Imported by**: console_scripts entry `quantflow = "quantflow.cli.main:app"` (pyproject.toml:56-57).

## Notes

- **8 commands** (CLAUDE.md lists 6; actual is 8): `download` (L84), `research` (L151), `optimize` (L217), `validate` (L288), `run` (L444), `benchmark` (L631), `station` (L1034), `status` (L1046).
- `benchmark` (L631): comprehensive perf baseline across data/indicators/research/validation/runtime/execution paths with threshold gates.
- `station` (L1034) launches `web.app.run_station`; `run` (L444) constructs `TradingSession` (L491).
- **Cred enforcement**: `_load_gateway_config_from_env` (L61-81) requires `OKX_API_KEY`/`OKX_SECRET`/`OKX_PASSPHRASE` for non-paper mode, raises `typer.BadParameter` if missing.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
