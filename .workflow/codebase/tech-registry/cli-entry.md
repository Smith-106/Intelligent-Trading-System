# TC-008 — CliEntry

| Field | Value |
|-------|-------|
| **ID** | TC-008 |
| **Type** | cli |
| **Features** | FT-002 (CLI) |

## Code Locations

- `quantflow/cli/main.py` — Typer + Rich app (`no_args_is_help=True`, rich markup mode L47-52); 9 commands
- `quantflow/cli/__init__.py`

## Exported Symbols

`ai`, `benchmark`, `download`, `optimize`, `research`, `run`, `station`, `status`, `validate`

## Dependencies

- **Imports**: `common`, `data`, `strategy`, `execution`, `indicators`, `monitoring`, `web`. Composes all layers (entry point).
- **Imported by**: console_scripts entry `quantflow = "quantflow.cli.main:app"` (pyproject.toml:56-57).

## Notes

- **9 commands** (CLAUDE.md lists 6; actual is 9): `download` (L87), `research` (L156), `optimize` (L222), `validate` (L293), `run` (L506), `benchmark` (L758), `station` (L1162), `ai` (L1174), `status` (L1242).
- `benchmark` (L758): comprehensive perf baseline across data/indicators/research/validation/runtime/execution paths with threshold gates.
- `station` (L1162) launches `web.app.run_station`; `run` (L506) constructs `TradingSession` (injects `monitoring_sink=create_default_sink()` at L554; standalone `ExecutionEngine` benchmark at L1025 also injects).
- `ai rdagent` (L1174): Qlib RD-Agent factor mining skeleton; dependency guard for optional qlib `[ml]` extra (absent → install hint, present → Alpha158-style baseline IC eval). Full LLM factor search is a future layer.
- **validate methods** (L298): `cpcv | dsr | pbo | wfo | full | gate | lookahead | stress`. `lookahead` (L348) = static look-ahead leak scan (no data needed); `stress` = path-level Monte Carlo (trade-shuffle + returns-bootstrap, diagnostic not gate — F5).
- **Cred enforcement**: `_load_gateway_config_from_env` (L61-81) requires `OKX_API_KEY`/`OKX_SECRET`/`OKX_PASSPHRASE` for non-paper mode, raises `typer.BadParameter` if missing. Cred-bearing exceptions redacted before surfacing (ISS-037 RP2, L145).
- **ISS-019/044 sink injection**: `cli/main.py` is the high-level injection point for `MonitoringSink` — `create_default_sink()` (from `monitoring/sink.py`, lazy-imported L529/803) is passed into `TradingSession` (L554, L977) and `ExecutionEngine` (L1025). Lower layers never import `quantflow.monitoring.*` directly.

*Auto-generated by codebase-refresh at 2026-07-25T00:00:00Z*
