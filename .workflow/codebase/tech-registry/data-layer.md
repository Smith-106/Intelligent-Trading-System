# TC-001 — DataLayer

| Field | Value |
|-------|-------|
| **ID** | TC-001 |
| **Type** | L1-data |
| **Features** | FT-007 (Data Layer) |

## Code Locations

- `quantflow/data/fetcher.py` — `DataFetcher` (CCXT async OKX, REST+WebSocket, `TIMEFRAMES`)
- `quantflow/data/cleaner.py` — `clean_ohlcv`, `validate_no_future_leak`
- `quantflow/data/store.py:39` — `DataStore` (Hive-partitioned Parquet symbol/year/month + DuckDB query; write path validates symbol)
- `quantflow/data/feature_store.py:17` — `FeatureStore` (point-in-time safe, no future leak; lazy-imports `IndicatorEngine`)
- `quantflow/data/redis_cache.py:16` — `RedisCache` (real-time ticker cache, `TICKER_TTL=60`; **reads raise `DataError` on client=None/failure — ISS-015, no silent cache-miss**)
- `quantflow/data/mtf_aligner.py:15` — `MTFAligner` (multi-timeframe alignment)

## Exported Symbols

`DataFetcher`, `DataStore`, `FeatureStore`, `MTFAligner`, `MTFData`, `RedisCache`, `clean_ohlcv`, `validate_no_future_leak`

## Dependencies

- **Imports**: only `common` (`validators`, `config`, `exceptions`) — clean, no upward leakage.
- **Imported by**: `cli/`, `strategy/` (catalog, research), `web/` (service).

## Notes

- `validate_symbol` / `validate_columns` from `common/validators.py` gate every symbol→path/glob site (read, write, in-place transform) per anti-path-traversal spec (REV-008).
- `feature_store.py:32` enforces point-in-time queries (`end=timestamp`) to prevent look-ahead data leakage.
- **ISS-027 (refactor)**: `fetcher.get_last_timestamp` (fetcher.py:159-167) delegates to `DataStore.get_last_timestamp` — eliminates the duplicate glob query (different glob depth, separate symbol/timeframe validation) that could drift from DataStore's read path. Validation moves into DataStore so a future caller is safe.
- **ISS-034 (perf)**: `DataStore.save` (store.py:84) has an append-only fast path — the common live/incremental case (all new bar timestamps > stored max, both sides already sorted) skips the O(n) `drop_duplicates` + `sort_values` + `reset_index` rewrite of the entire month partition. Falls back to full dedup+sort only when overlap is detected.
- **ISS-015 + GP1 fail-silent cluster (commits `7f7eafe` + `d76ceb9`, ISS-20260723-013/014/016)**: store layer exception paths now `raise DataError` instead of returning empty/None (fail-silent anti-pattern). `RedisCache` reads no longer fake a cache miss on `client=None`/failure — they raise `DataError`; callers (`web/service.py`, `strategy/`) wrap in `try/except DataError` for graceful degradation. "Legitimate no-data" (source None / dir doesn't exist) is distinguished from "query failure" (DuckDB execution exception) — only the latter raises. Affected: `DataStore.query`/`get_date_range`/`get_last_timestamp` + `FeatureStore.load_features` (mirrors store.query). Aligns with coding spec S-20260724-elsu (fail-silent fallback 不可复用合法结果值).

*Auto-generated by codebase-refresh at 2026-07-25T00:00:00Z; codebase-refresh 2026-07-28 ISS-015 + GP1 fail-silent cluster*
