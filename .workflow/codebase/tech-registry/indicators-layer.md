# TC-002 — IndicatorsLayer

| Field | Value |
|-------|-------|
| **ID** | TC-002 |
| **Type** | L2-indicators |
| **Features** | FT-009 (Indicators — 27 factors) |

## Code Locations

- `quantflow/indicators/base.py:14` — `FactorBase(ABC)` (`compute(df, **params) -> Series`), `FactorRegistry` (L43), global `registry` (L72)
- `quantflow/indicators/engine.py:9` — `IndicatorEngine` (`batch_calculate`, `compute_all`, `FACTOR_NAMES`, `_register_wave_factors`)
- `quantflow/indicators/trend.py` — sma, ema, dema, macd, supertrend, adx
- `quantflow/indicators/momentum.py` — rsi, stochastic_rsi, stochastic, williams_r
- `quantflow/indicators/volatility.py` — atr, true_range, bollinger_bands, keltner_channel, donchian_channel
- `quantflow/indicators/volume.py` — obv, vwap, mfi, volume_sma, volume_ratio
- `quantflow/indicators/regime.py:12` — `MarketRegimeDetector` (strategy gating)
- Elliott Wave subsystem: `elliott_wave.py`, `zigzag.py`, `wave_models.py`, `wave_identifier.py`, `wave_channel.py`, `fibonacci.py`, `divergence.py`, `critical_level.py`

## Exported Symbols

`FactorBase`, `FactorRegistry`, `IndicatorEngine`, `MarketRegimeDetector`, `WavePattern`, `AnalysisMode`, `WaveCount`

## Dependencies

- **Imports**: intra-`indicators` + stdlib/pandas only. **Zero imports of higher layers** — cleanest layer.
- **Imported by**: `data/feature_store` (lazy), `strategy/`, `signal/`, `execution/scaling_position_sizer` (anomaly — see TC-005 notes).

## Notes

- **27 registered factors** (engine.py:33-67): 21 base + 6 Elliott Wave. CLAUDE.md says "21 factors" — doc drift; engine registers 27.
- Pure pandas/numpy, no external TA library (TA-Lib optional). `MarketRegimeDetector.update()` feeds strategy gating in `TradingSession`.

*Auto-generated by codebase-rebuild at 2026-07-15T22:35:00Z*
