# TC-003 — StrategyLayer

| Field | Value |
|-------|-------|
| **ID** | TC-003 |
| **Type** | L3-strategy |
| **Features** | FT-001 (Strategies), FT-003 (Research Pipeline), FT-004 (Anti-Overfitting Validation), FT-010 (AI Layer) |
| **Last Updated** | 2026-07-29T06:38:45Z |

## Code Locations

- `quantflow/strategy/base.py` — `StrategyBase`, `StrategyContext` (dual-mode contract)
- `quantflow/strategy/engine.py` — `TradingSession` orchestrator
- `quantflow/strategy/catalog.py` — strategy discovery + YAML config loader
- `quantflow/strategy/ai_factors.py` — `AIFactorEngine` (Meta-Labeling, exported but unwired)
- `quantflow/strategy/sentiment.py` — `SentimentAnalyzer`, `NewsCollector` (FinBERT; implemented but unexported/unwired)
- `quantflow/strategy/rd_agent.py` — `RDAgentRunner` (Qlib RD-Agent CLI skeleton, dependency guard)
- `quantflow/strategy/research/` — `backtest.py` (`BacktestEngine`), `optimizer.py` (`StrategyOptimizer`), `report.py`
- `quantflow/strategy/templates/` — 7 strategy implementations
- `quantflow/strategy/validation/` — `cpcv`, `dsr`, `pbo`, `wfo`, `gate`, `barriers`, `lookahead`, `monte_carlo`, `signal_quality`

## Exported Symbols (selected)

`StrategyBase`, `StrategyContext`, `TradingSession`, `BacktestEngine`, `BacktestResult`,
`StrategyOptimizer`, `generate_report`, `validation_gate`, `cpcv_backtest`, `deflated_sharpe_ratio`,
`probability_of_overfitting`, `walk_forward_optimization`, `triple_barrier_labels`, `minimum_track_record_length`,
`monte_carlo_stress`, `trade_shuffle_stress`, `returns_bootstrap_stress`, `LookaheadReport`, `LookaheadFinding`,
`trend_following`/`mean_reversion`/`elliott_wave`/`volatility_breakout`/`funding_rate`/`momentum_rotation`/`ml_ensemble` strategy classes,
`AIFactorEngine`, `MLEnsembleStrategy`, `SentimentAnalyzer`, `NewsCollector`, `RDAgentRunner`, `RDAgentConfig`,
`QlibNotAvailableError`.

## Dependencies

- Upstream: `quantflow/common`, `quantflow/data`, `quantflow/indicators`, `quantflow/signal` (RiskEngine/PositionSizer via TradingSession), `quantflow/execution` (Gateway via session).
- Downstream consumers: L4 Signal (called by TradingSession), L5 Execution (called by TradingSession), CLI (`quantflow/cli/main.py`), Web Station (`quantflow/web/service.py`).
- External: Optuna, pandas, numpy, sklearn (ai_factors/ml_ensemble), transformers (sentiment, optional), qlib (rd_agent, optional).

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
