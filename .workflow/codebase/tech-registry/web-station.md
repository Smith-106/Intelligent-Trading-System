# TC-009 — WebStation

| Field | Value |
|-------|-------|
| **ID** | TC-009 |
| **Type** | web-presentation |
| **Features** | FT-008 (QuantFlow Station Web UI) |
| **Last Updated** | 2026-07-29T06:38:45Z |

## Code Locations

- `quantflow/web/app.py` — `create_app`, `run_station` (aiohttp SPA)
- `quantflow/web/security.py` — `same_origin_guard` (Bearer hmac + CSRF)
- `quantflow/web/service.py` — `StationService`
- `quantflow/web/session_manager.py` — `StationSessionManager`, `SessionRuntime`, `SessionStartRequest`
- `quantflow/web/history.py` — `StationHistoryStore`
- `quantflow/web/rate_limit.py` — `RateLimiter`, `rate_limit_middleware`
- `quantflow/web/static/__init__.py`

## Exported Symbols

`create_app`, `run_station`, `same_origin_guard`, `StationService`, `StationSessionManager`, `SessionRuntime`,
`SessionStartRequest`, `StationHistoryStore`, `RateLimiter`, `rate_limit_middleware`, `DataDownloadRequest`,
`DataSourceTagRequest`, `ResearchRequest`, `ValidationRequest`, `format_data_source_label`.

## Dependencies

- Upstream: `quantflow/common` (models, redaction), `quantflow/strategy` (catalog, research, validation, TradingSession), `quantflow/data`, `quantflow/execution` (session control).
- Downstream consumers: user entry point (browser); CLI `station` command launches it.
- External: aiohttp. Security: Bearer `QUANTFLOW_STATION_TOKEN` (hmac.compare_digest), CSRF Origin-Host, bind-boundary guard for non-loopback, 256KiB body cap, path/secret redaction.

---

*Auto-generated by codebase-rebuild at 2026-07-29T06:38:45Z*
