# Wiki health 90→100 (2026-08-12)

## Score model
`score = 100 - 2*brokenLinks - orphans - 3*missingTitles`

## Fixes
1. **Real broken related**: `DOC-monitoring-sink-protocol` related `DOC-research-go-panel-export` → `knowhow-doc-research-go-panel-export` (resolves to wiki id).
2. **False positives (local sessions)**: sealed `20260805-maestro-knowledge-sync` run notes contained `[[..]]` / `[["overview"]]` matched by LINK_RE; neutralized to fullwidth `⟦…⟧` in session projections (sessions are gitignored; local wiki index only).

## Verify
```bash
maestro wiki health   # 100/100, broken 0, orphans 0
maestro kg sync && maestro kg health
```
