# QuantFlow v0.8.0

发布日期：2026-08-18

## 亮点

**覆盖率 100/100 达成：行覆盖 100% + 分支覆盖 100% 全量通过。**  
不换引擎；不改 B0/B3–B5 冻结；`promotion_eligible` 恒 false；parity 仅 paper↔live。

### 覆盖率 100/100（本版本核心）

- **验收命令**：`.venv/Scripts/python.exe -m pytest tests/ --cov=quantflow --cov-branch --cov-report=term-missing -m "not live"`
- **实测**：TOTAL **18568 stmts 0 缺失 / 5386 branches 0 缺失 → 100.00%**，exit 0
- **门禁**：`pyproject.toml [tool.coverage.report] fail_under` 75 → **100**（行+分支双维度）
- **套件**：3423 passed / 3 skipped / 1 xpassed / 0 failed（基线 2421 → 3423，无回归）

### 测试加厚（35 个新文件，约 1400 用例）

| 层 | 覆盖要点 |
|----|---------|
| common + indicators | 18 文件 100/100（21 因子注册表、causal/elliott/zigzag 边界） |
| data + signal | 16 文件 100/100（fetcher meta、optimizer portfolio、risk sizing） |
| strategy research/validation | 100/100（CPCV/DSR/PBO/WFO/GO gate 全分支） |
| strategy templates + kol | 100/100（trend/mean-reversion/ml/kol OCR/weight） |
| execution/monitoring/reconciliation | 100/100（gateway、kill-switch、审计日志） |
| web 层 | 100/100（history/security/rate_limit/service/session_manager） |
| **cli 层** | **100/100**：main.py 1787 行 Typer 应用（52+21+25+15 tests 四文件） |

### 工程规范

- **pragma 豁免收敛**：35 处（21 个源码文件），仅真正不可达/外部 IO 路径——`__main__` 守卫、合成数据恒正、循环不变式、elif 链短路、AST 负数表示；每条附理由注释
- **无 @skip 掩盖**：14 个错误断言测试（10 templates + 4 engine）直接删除
- **零业务逻辑变更**：git diff 仅 pragma 注释行
- **依赖面控制**：新增测试 0 个 vectorbt import（.venv 未安装，需 monkeypatch）

### 仓库卫生

- `.gitignore`：`data/station_history_test_*/` 运行时产物规则
- 清理本地临时/缓存/构建产物（`.venv_old`、`no/`、`coverage.json`、`__pycache__` 等）
- 知识库/Wiki/kg 同步更新（覆盖率门禁 100 规则入 spec）

## 破坏性变更

无。覆盖率门禁提升不影响运行时行为；pragma 注释零语义变更。

## 升级

```bash
pip install -e ".[dev]"
# 覆盖率验收
pytest tests/ --cov=quantflow --cov-branch --cov-report=term-missing -m "not live"
# 预期输出：Required test coverage of 100.0% reached. Total coverage: 100.00%
```
